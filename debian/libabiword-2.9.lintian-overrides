# Upstream build system do not generate symlink to libabiword-2.9.so.N
# libabiword-2.9.so is a library itself, (binary file) not a symlink.
dev-pkg-without-shlib-symlink

# symbols are not very useful for CPP,
# see http://wiki.debian.org/UsingSymbolsFiles#See_also
no-symbols-control-file
