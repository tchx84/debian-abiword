Author: Dmitry Smirnov <onlyjob@member.fsf.org>
Last-Update: 2012-01-21
Forwarded: no
Description: prevent breaking of source tree
 Without this patch abiword breaks it's own source tree by removing
 some essential non-autogenerated files which break the second build


--- a/Makefile.am
+++ b/Makefile.am
@@ -54,10 +54,6 @@
 
 DISTCLEANFILES = \
 	abiword-$(ABIWORD_SERIES).pc \
-	plugin-builtin.m4 \
-	plugin-configure.m4 \
-	plugin-list.m4 \
-	plugin-makefiles.m4 \
 	doltcompile \
 	doltlibtool
 
--- a/Makefile.in
+++ b/Makefile.in
@@ -485,10 +485,6 @@
 
 DISTCLEANFILES = \
 	abiword-$(ABIWORD_SERIES).pc \
-	plugin-builtin.m4 \
-	plugin-configure.m4 \
-	plugin-list.m4 \
-	plugin-makefiles.m4 \
 	doltcompile \
 	doltlibtool
 
